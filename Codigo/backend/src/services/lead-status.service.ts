export class LeadStatusService {
  async updateLeadStatusFromConversation(_conversationId: number): Promise<void> {
    // Minimal no-op implementation to keep conversation flows running
    // while the lead synchronization layer is absent in this workspace.
  }
}
